# kovia-admin

Standalone React admin application for Kovia.

## Scripts

- `pnpm dev` starts the Vite dev server.
- `pnpm build` creates a production build.
- `pnpm preview` serves the production build locally.

## Environment

Copy `.env.example` into `.env` and update values if needed.
